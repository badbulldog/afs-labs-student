apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-deployment
  namespace: ylee
  labels:
    app: db
spec:
  replicas: 1
  strategy:
    type: Recreate
  minReadySeconds: 20
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
      - envFrom:
        - secretRef:
            name: db-secret
        name: db-deployment
        image: docker pull harbor.dev.afsmtddso.com/ylee-harbor-project/db:971c881-f372d9f
        - containerPort: 5432
